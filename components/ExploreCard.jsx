"use client";
import { motion } from "framer-motion";
import styles from "../styles";
import { fadeIn } from "../utils/motion";

const ExploreCard = ({ id, imgUrl, title, i, active, handleClick }) => (
  <div>Explore Card</div>
);

export default ExploreCard;
